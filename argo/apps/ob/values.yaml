---
cloud:
  id: dev-aic-ob-private-id
  key: dev-aic-ob-private-key

cors:
  originEnds: localhost

environment:
  # CDK value: (Cloud Developer's Kit) development identity platform
  # CDM value: CDM (Cloud Deployment Model) identity cloud platform
  # FIDC value: Cloud Deployment Model) identity cloud platform
  cloudType: CDK
  sapigType: ob
  kustomizeFolder: dev

externalCert:
  projectId: example-project
  mtls:
    certPrefix: dev
    secretName: mtls-tls-cert
  sapig:
    certPrefix: dev
    secretName: core-tls-cert
  ttd:
    certPrefix: dev
    secretName: ttd-tls-cert

fidcConfigurator:
  enabled: true
  branch: HEAD
  repo: europe-west4-docker.pkg.dev/sbat-gcr-develop/core-docker-artifact/core/secureopenbanking-uk-iam-initializer
  tag: latest

ig:
  tag: latest
  branch: HEAD
  truststore:
    # Reusing the secret that configures AM to trust CAs for OAuth 2.0 tls_client_auth
    googleSecretName: am-oauth2-ca-certs
  ob:
    signingkey:
      googleSecretName: ig-ob-signing-key

ingress:
  domain: localhost

remoteConsentServer:
  branch: HEAD
  repo: europe-west4-docker.pkg.dev/sbat-gcr-develop/core-docker-artifact/core/core-openbanking-uk-rcs
  tag: latest

remoteConsentServiceUserInterface:
  branch: HEAD
  repo: europe-west4-docker.pkg.dev/sbat-gcr-develop/core-docker-artifact/core/ui/rcs
  tag: latest

spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  source:
    targetRevision: HEAD
  syncPolicy: 
    automated:
      prune: true
      selfHeal: true

testFacilityBank:
  branch: HEAD
  repo: europe-west4-docker.pkg.dev/sbat-gcr-develop/core-docker-artifact/core/core-openbanking-uk-rs
  tag: latest

testTrustedDirectory:
  tag: latest
  branch: HEAD

testUserAccountCreator:
  branch: HEAD
  deployment:
    type: CronJob
  repo: europe-west4-docker.pkg.dev/sbat-gcr-develop/core-docker-artifact/core/core-test-data-initializer
  tag: latest